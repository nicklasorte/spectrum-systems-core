{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spectrum-systems-core/contracts/schemas/paper/technical_claim.schema.json",
  "title": "technical_claim",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "claim_id",
    "source_id",
    "source_unit_id",
    "source_turn_ids",
    "source_turn_validation",
    "source_excerpt",
    "claim_text",
    "claim_type",
    "materiality",
    "supported_by_evidence_ids",
    "contradicted_by_claim_ids",
    "extraction_model",
    "extraction_temperature",
    "status",
    "created_at",
    "provenance"
  ],
  "properties": {
    "schema_version": { "const": "1.1.0" },
    "claim_id": { "type": "string", "format": "uuid" },
    "source_id": { "type": "string", "minLength": 1 },
    "source_unit_id": { "type": "string", "format": "uuid" },
    "source_turn_ids": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "format": "uuid" },
      "description": "IDs of speaker-turn chunks this claim was extracted from. Required. Every extracted item must cite its source. Array form of source_unit_id; supports future multi-turn extraction. Phase M.1."
    },
    "source_turn_validation": {
      "enum": ["verified", "invalid", "missing"],
      "description": "Post-extraction validation status of source_turn_ids. verified=all IDs were in the input units; invalid=at least one ID was not in the input units (item kept for human review); missing=item had no source_turn_ids (such items are omitted from the artifact). Phase M.1."
    },
    "source_excerpt": { "type": "string", "minLength": 10 },
    "claim_text": { "type": "string", "minLength": 10 },
    "claim_type": {
      "enum": ["factual", "methodological", "predictive", "normative"]
    },
    "materiality": { "enum": ["high", "medium", "low"] },
    "supported_by_evidence_ids": {
      "type": "array",
      "items": { "type": "string", "format": "uuid" }
    },
    "contradicted_by_claim_ids": {
      "type": "array",
      "items": { "type": "string", "format": "uuid" }
    },
    "extraction_model": { "type": "string", "minLength": 1 },
    "extraction_temperature": {
      "type": "number",
      "minimum": 0,
      "maximum": 0
    },
    "status": {
      "enum": ["candidate", "evidenced", "contradicted", "superseded"]
    },
    "created_at": { "type": "string", "format": "date-time" },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "produced_by",
        "input_artifact_ids",
        "execution_fingerprint_hash"
      ],
      "properties": {
        "produced_by": {
          "type": "object",
          "additionalProperties": false,
          "required": ["component", "version"],
          "properties": {
            "component": { "type": "string", "minLength": 1 },
            "version": { "type": "string", "minLength": 1 }
          }
        },
        "input_artifact_ids": {
          "type": "array",
          "items": { "type": "string" }
        },
        "execution_fingerprint_hash": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        }
      }
    }
  }
}
